var fs = require("fs");
var input = fs
  .readFileSync(`${__dirname}/TestCase/sugarFactory`)
  .toString()
  .trim()
  .split("\n");

let N = Number(input);
let five = 0;
let three = 0;

while (N > 0) {
  if (N % 5 === 0) {
  }
}
